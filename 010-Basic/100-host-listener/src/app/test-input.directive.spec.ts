import { TestInputDirective } from './test-input.directive';

describe('TestInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TestInputDirective();
    expect(directive).toBeTruthy();
  });
});
